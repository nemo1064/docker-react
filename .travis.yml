language: generic
sudo: required
services:
  - docker
before_install:
  - docker build -t nemo1064/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true nemo1064/docker-react npm run test -- --coverage